import * as R from 'ramda'

import book from './mockBook'
import categories from  './mockCategories'
import user from "./users"

export  const  fetchUser = async (email, password) => {
    return new Promise((resolve) => {
        const users = user.find(x => x.email === email && x.password === password);
        console.log(users);
        resolve(users)
    })
};

export  const  addUser = async (name, surname, phone, email, password) => {
    return new Promise((resolve) => {

        console.log(user);
        resolve(user)
    })
};

export  const  fetchBook = async() => {
    return new Promise(resolve => {
        resolve(book)
    })
};

export  const  loadMoreBook = async({offset}) => {
    return new Promise(resolve => {
        resolve(book)
    })
};

export  const  fetchBooksById = async (id) => {
    return new Promise((resolve, reject) => {
        const books = R.find(R.propEq('id', id), book);
        resolve(books)
    })
};

export const fetchCategories = async  () => {
    return new Promise(resolve => {
        resolve(categories)
    })
};